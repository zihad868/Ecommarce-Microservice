{
  "name": "ecommerce-microservice-root",
  "version": "1.0.0",
  "description": "Root orchestrator for Ecommerce Microservices",
  "scripts": {
    "install:all": "npm install --prefix auth-service && npm install --prefix product-service && npm install --prefix order-service",
    "build": "npm run build --prefix auth-service && npm run build --prefix product-service && npm run build --prefix order-service",
    "db:generate": "npm run db:generate --prefix auth-service && npm run db:generate --prefix product-service && npm run db:generate --prefix order-service",
    "db:push": "npm run db:push --prefix auth-service && npm run db:push --prefix product-service && npm run db:push --prefix order-service",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "private": true
}
